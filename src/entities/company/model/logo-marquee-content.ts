import type { Locale } from "@/shared/lib/i18n"

export const logoMarqueeContentByLocale: Record<Locale, { caption: string }> = {
  ko: { caption: "INEX와 함께하는 파트너사" },
  en: { caption: "Partners working with INEX" },
}
